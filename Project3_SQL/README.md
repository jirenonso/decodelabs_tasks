Imported the cleaned dataset into SQL Server. 
Checked for correct data types, constraints, column headers to ensure accuracy and successful import of flat file.
Extract business insights based on KPIs including sales category, customer behaviour, product performance and monthly/yearly trend.
Used SQL Statements SELECT, WHERE, GROUP BY, HAVING and aggregate, Scalar functions (COUNT, SUM, AVG, ROUND).

Tools and skills: SQL Server, SQL Server Management Studio (SSMS), DDL, Data analysis
